[package]
name = "bundt"
version = "0.0.0"
edition = "2024"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["process", "io-std", "io-util", "rt-multi-thread", "macros"] }

[dev-dependencies]
which = "7"
